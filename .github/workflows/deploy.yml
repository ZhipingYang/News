name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master
      - main
    paths:
      - '20**/**/*.md'      # 监听所有日期文件夹中的 markdown 文件
      - 'static-site/**'     # 监听静态网站生成器变更
      - 'package.json'
      - '.github/workflows/deploy.yml'

  # 允许手动触发
  workflow_dispatch:

# 设置权限
permissions:
  contents: read
  pages: write
  id-token: write

# 只允许一个部署同时进行
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout代码
        uses: actions/checkout@v4
      
      - name: 🔧 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: 📦 安装依赖
        run: npm ci
      
      - name: 🏗️ 构建静态网站
        run: npm run build
      
      - name: 📊 构建统计
        run: |
          echo "=== 构建完成 ==="
          echo "生成的文件："
          ls -lh docs/
          echo ""
          echo "日期文件夹："
          ls -d 20*/ 2>/dev/null || echo "暂无日期文件夹"
      
      - name: 📤 上传构建产物
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: 🚀 部署到 GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
      
      - name: ✅ 部署完成
        run: |
          echo "🎉 部署成功！"
          echo "网站地址：${{ steps.deployment.outputs.page_url }}"

